import { suiteKontenLanding } from '../helpers/landing'

/**
 * Konten /id. Isi suite-nya ada di `helpers/landing.ts`, dipakai bersama
 * dengan landing-en.spec.ts — nilai yang diharapkan dibaca dari database,
 * bukan dari seed yang dituliskan ulang di sini.
 */
suiteKontenLanding('id')
